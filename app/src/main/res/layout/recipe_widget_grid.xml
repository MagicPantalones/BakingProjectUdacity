<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/bullet_point_widget"
        android:layout_width="12dp"
        android:layout_height="12dp"
        android:src="@drawable/donut_only_icon_56pt"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/ingredient_text_widget"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:gravity="center_vertical"
        android:lines="2"
        android:paddingEnd="16dp"
        android:paddingStart="8dp"
        android:textSize="12sp"
        app:layout_constraintBottom_toBottomOf="@+id/bullet_point_widget"
        app:layout_constraintStart_toEndOf="@+id/bullet_point_widget"
        app:layout_constraintTop_toTopOf="@+id/bullet_point_widget"
        tools:text="4 tblsp vanilla extract, divided" />

</android.support.constraint.ConstraintLayout>